// Generated by CoffeeScript 1.10.0
(function() {
  var config, getIcon, misc, weather;

  misc = require('../lib/misc');

  config = require('../lib/config');

  getIcon = function(type) {
    switch (type) {
      case "01d":
        return "☀️";
      case "01n":
        return "";
      case "02d":
        return "🌤";
      case "02n":
        return "🌤";
      case "03d":
        return "☁️";
      case "03n":
        return "☁️";
      case "04d":
        return "☁️";
      case "04n":
        return "☁️";
      case "09d":
        return "🌧";
      case "09n":
        return "🌧";
      case "10d":
        return "🌦";
      case "10n":
        return "🌦";
      case "11d":
        return "🌩";
      case "11n":
        return "🌩";
      case "13d":
        return "🌨";
      case "13n":
        return "🌨";
      case "50d":
        return "🌫";
      case "50n":
        return "🌫";
    }
  };

  weather = function(cityName) {
    return misc.get('http://api.openweathermap.org/data/2.5/weather', {
      qs: {
        q: cityName,
        units: 'metric',
        appid: config.options.weathermap
      },
      json: true
    }).then(function(res) {
      if (res.cod !== 200) {
        throw new Error(res.message);
      }
      return res;
    });
  };

  module.exports = {
    name: 'Weather',
    pattern: /!(weather|погода)(?: (.+))?/,
    isConf: true,
    onMsg: function(msg, safe) {
      var res, txt;
      txt = msg.match[2];
      if (txt == null) {
        return;
      }
      res = weather(txt);
      return safe(res).then(function(data) {
        var desc, emoji, icon, obj;
        icon = getIcon(data['weather'][0]['icon']);
        emoji = (
          obj = {},
          obj["" + icon] = data['main']['temp'] + " °C",
          obj["💦"] = data['main']['humidity'] + "%",
          obj["💨"] = data['wind']['speed'] + " km/h / " + data['wind']['deg'] + " deg",
          obj["🌅"] = "" + (new Date(data['sys']['sunrise'] * 1000).toLocaleTimeString()),
          obj["🌄"] = "" + (new Date(data['sys']['sunset'] * 1000).toLocaleTimeString()),
          obj
        );
        desc = data['name'] + ", " + data['sys']['country'] + " - " + data['weather'][0]['main'] + "\n" + data['weather'][0]['description'];
        Object.keys(emoji).map(function(e) {
          return desc += "\n" + e + ": " + emoji[e];
        });
        return msg.reply(desc);
      });
    },
    onError: function(msg) {
      return msg.reply('Город не найден.');
    }
  };

}).call(this);

//# sourceMappingURL=weather.js.map
